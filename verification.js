// Assume this function verifies the code and then redirects to dashboard.html
function verifyCode() {
    // Verification code logic...

    // Redirect to dashboard.html after verification
    window.location.href = 'dashboard.html';
}
